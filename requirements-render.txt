# Production requirements for Render deployment
# Trimmed from full requirements.txt - removes heavy ML libs that aren't needed
# for the core Flask API to run. Add them back if your routes actually use them.

# Core Flask
Flask==3.0.0
Flask-CORS==4.0.0
Flask-SQLAlchemy==3.1.1
Flask-JWT-Extended==4.5.3
Werkzeug==3.0.1

# Database
sqlalchemy==2.0.23

# Auth
PyJWT==2.8.1
bcrypt==4.1.1

# Env & Utils
python-dotenv==1.0.0
python-multipart==0.0.6
requests==2.31.0

# WSGI Server
gunicorn==22.0.0

# Image processing (if chatbot needs it)
Pillow==10.1.0

# Data processing (lightweight)
numpy==1.24.3
joblib==1.3.2
